package com.radex392.temporalrelativity.proxy;

public abstract class CommonProxy implements IProxy
{
    public void registerRenderThings(){
        //client proxy will use this, probably not server proxy
    }
}
